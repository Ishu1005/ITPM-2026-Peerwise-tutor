📁 Project Setup

1. Clone the repo:
   git clone https://github.com/ThaminduSulakshana/ITP-Spices-2025.git
   cd ITP-Spices-2025

2. Create `.env` in the /server folder:

   MONGO_URI=mongodb+srv://test69:test69@cluster0.blht0aw.mongodb.net/yourDatabaseName?retryWrites=true&w=majority&appName=Cluster0
   JWT_SECRET=mySuperSecretKey12345!
   PORT=5000

   🔒 For test use only — replace with secure values before deployment.

3. Install dependencies:

   Backend:
   cd server
   npm install

   Frontend:
   cd ../client
   npm install

4. Run the app:

   Start backend:
   cd server
   npm start

   Start frontend:
   cd ../client
   npm start


- Do NOT commit `.env` to GitHub.
- Use `.env.example` to share structure safely.
- For production, replace all test values.
